import { MonoText } from '@/components/shared/StyledText';

export const RegisterButton = () => (
  <MonoText style={{ marginHorizontal: 20, fontSize: 20, fontWeight: 'bold' }}>
    Register
  </MonoText>
);
