import { StyleSheet } from 'react-native';

export const shadowsStyle = StyleSheet.create({
  containerShadow: {
    shadowColor: '#00000026',
    shadowOffset: {
      width: 0,
      height: 3,
    },
    shadowOpacity: 1,
    shadowRadius: 5,
    elevation: 5,
  },
});
