import { StyleSheet } from 'react-native';

export const chatContainerStyle = StyleSheet.create({
  twoColumnsContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 10,
  },
});
